package Interview_22_06_2023;

import org.openqa.selenium.Alert;
import org.openqa.selenium.By;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chrome.ChromeOptions;

public class alert_1 {
 public static void main(String args[])
 {

	 
	 ChromeDriver driver=new ChromeDriver();
	/*  driver.get("https://demo.automationtesting.in/Alerts.html");
	 // driver.findElement(By.xpath("//button[contains(text(),'click the button to display an')]")).click();
	//   driver.switchTo().alert().accept();
	  // driver.close();
   //  driver.findElement(By.xpath("//a[normalize-space()='Alert with OK & Cancel']")).click();
     // driver.findElement(By.xpath("//button[normalize-space()='click the button to display a confirm box']")).click();
      
 //    driver.switchTo().alert().dismiss(); // Closed by cancel button with alert window
	 
	  //Alert with textBox
	  driver.findElement(By.xpath("//a[normalize-space()='Alert with Textbox']")).click();
	  driver.findElement(By.xpath("//button[normalize-space()='click the button to demonstrate the prompt box']")).click();
	  
	  Alert wind=driver.switchTo().alert();
	   wind.sendKeys("eyyy");
	   System.out.println(wind.getText());
	   wind.accept();*/
	// driver.get(" http://admin:admi@the-internet.herokuapp.com/basic_auth"); // Used with user name and password
	 
	// driver.get(" http://the-internet.herokuapp.com/basic_auth");
	 driver.get("https://www.redbus.in/");
	
	  // driver.quit();
	   	
	   
	   
	
	  
	  
	//a[normalize-space()='Alert with Textbox']
     
     
     
     
	   
	  
	 
 }

}
